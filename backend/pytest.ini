[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
# Only classes named "*Tests" are pytest test classes here — this repo's domain model
# legitimately has TestSession/TestAnswer/TestSessionRepository etc. (aptitude assessment
# "tests"), which pytest would otherwise warn about trying to collect.
python_classes = *Tests
